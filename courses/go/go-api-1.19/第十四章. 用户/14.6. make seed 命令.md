# 14.6. make seed 命令

原文链接：https://learnku.com/courses/go-api/1.19/make-seed-command/13560

## 说明

本节将开发 make seed 命令。

## 1. 创建命令

app/cmd/make/make_seeder.go

```go
package make

import (
	"fmt"

	"github.com/spf13/cobra"
)

var CmdMakeSeeder = &cobra.Command{
	Use:   "seeder",
	Short: "Create seeder file, example:  make seeder user",
	Run:   runMakeSeeder,
	Args:  cobra.ExactArgs(1), // 只允许且必须传 1 个参数
}

func runMakeSeeder(cmd *cobra.Command, args []string) {

	// 格式化模型名称，返回一个 Model 对象
	model := makeModelFromString(args[0])

	// 拼接目标文件路径
	filePath := fmt.Sprintf("database/seeders/%s_seeder.go", model.TableName)

	// 基于模板创建文件（做好变量替换）
	createFileFromStub(filePath, "seeder", model)
}
```

## 2. 注册命令

app/cmd/make/make.go

```go
func init() {
    // 注册 make 的子命令
    CmdMake.AddCommand(
        .
        .
        .
        CmdMakeSeeder,
    )
}
```

## 3. 模板

app/cmd/make/stubs/seeder.stub

```go
package seeders

import (
    "fmt"
    "gohub/database/factories"
    "gohub/pkg/console"
    "gohub/pkg/logger"
    "gohub/pkg/seed"

    "gorm.io/gorm"
)

func init() {

    seed.Add("Seed{{StructNamePlural}}Table", func(db *gorm.DB) {

            {{VariableNamePlural}}  := factories.Make{{StructNamePlural}}(10)

            result := db.Table("{{TableName}}").Create(&{{VariableNamePlural}})

            if err := result.Error; err != nil {
                logger.LogIf(err)
                return
            }

            console.Success(fmt.Sprintf("Table [%v] %v rows seeded", result.Statement.Table, result.RowsAffected))
    })
}
```

## 4. 测试

运行命令：

```bash
$ go run main.go make seeder project
[database/seeders/projects_seeder.go] created.
```

请自行查看生成的文件。确认无误后请删除此文件：

```bash
$ rm -f database/seeders/projects_seeder.go
```

## 代码版本

本节功能开发完毕。开始下一节之前，先来为代码做下版本标记：

```bash
$ git add .
$ git commit -m "make seed 命令"
```
