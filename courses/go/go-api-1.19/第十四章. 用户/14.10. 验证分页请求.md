# 14.10. 验证分页请求

原文链接：https://learnku.com/courses/go-api/1.19/verify-paging-request/13564

## 说明

目前我们的分页请求中，允许用户请求这些参数：

- page=3 —— 请求第几页内容

- sort=id —— 排序字段和 order 字段配合使用

- order=asc —— 排序规则，asc 是正序，desc 是倒序

- per_page=10 —— 每一页多少内容

这些参数，除了 page 参数以外，其他几个参数都需要做请求验证。

规则如下：

- sort —— 设置白名单，例如说只允许 id 或者 updated_at 来做排序；

- order —— 设置白名单，排序规则一般就是正序和倒序；

- per_page —— 设置区间，少于 2 就没有分页的必要，而大于 100 请求太大，也太消耗服务器资源，故控制在 100 以内。

下面开始开发分页请求验证。

## 1. 请求验证

我们创建一个通用的 PaginationRequest 过滤规则，大部分情况下其他『列表』的API 都能使用。如果发现不适用，再创建专属的分页请求验证规则也可。

app/requests/pagination_request.go

```
package requests

import (
"github.com/gin-gonic/gin"
"github.com/thedevsaddam/govalidator"
)

type PaginationRequest struct {
Sort    string `valid:"sort" form:"sort"`
Order   string `valid:"order" form:"order"`
PerPage string `valid:"per_page" form:"per_page"`
}

func Pagination(data interface{}, c *gin.Context) map[string][]string {

rules := govalidator.MapData{
"sort":     []string{"in:id,created_at,updated_at"},
"order":    []string{"in:asc,desc"},
"per_page": []string{"numeric_between:2,100"},
}
messages := govalidator.MapData{
"sort": []string{
"in:排序字段仅支持 id,created_at,updated_at",
},
"order": []string{
"in:排序规则仅支持 asc（正序）,desc（倒序）",
},
"per_page": []string{
"numeric_between:每页条数的值介于 2~100 之间",
},
}
return validate(data, rules, messages)
}
```

## 2. 修改控制器方法

将 app/http/controllers/api/v1/users_controller.go 里的 Index 方法修改如下：

```
// Index 所有用户
func (ctrl *UsersController) Index(c *gin.Context) {
request := requests.PaginationRequest{}
if ok := requests.Validate(c, &request, requests.Pagination); !ok {
return
}

data, pager := user.Paginate(c, 10)
response.JSON(c, gin.H{
"data":  data,
"pager": pager,
})
}
```

## 3. 测试

Postman 新建一个测试的请求，这个请求不需要保存。请求链接如下：

```
http://localhost:3000/v1/users?page=2&sort=id&order=234&per_page=2
```

order 字段那里填入了非法的字符，发送请求后，会得到这样的响应：

```
{
"errors": {
"order": [
"排序规则仅支持 asc（正序）,desc（倒序）"
]
},
"message": "请求验证不通过，具体请查看 errors"
}
```

符合预期。

请自行测试其他验证规则。

## 代码版本

本节功能开发完毕。开始下一节之前，先来为代码做下版本标记：

```
$ git add .
$ git commit -m "验证分页请求"
```
