# 14.5. make factory 命令

原文链接：https://learnku.com/courses/go-api/1.19/make-factory-command/13559

## 说明

本节将创建 make factory 命令。

## 1. 创建命令

app/cmd/make/make_factory.go

```
package make

import (
"fmt"

"github.com/spf13/cobra"
)

var CmdMakeFactory = &cobra.Command{
Use:   "factory",
Short: "Create model's factory file, example: make factory user",
Run:   runMakeFactory,
Args:  cobra.ExactArgs(1), // 只允许且必须传 1 个参数
}

func runMakeFactory(cmd *cobra.Command, args []string) {

// 格式化模型名称，返回一个 Model 对象
model := makeModelFromString(args[0])

// 拼接目标文件路径
filePath := fmt.Sprintf("database/factories/%s_factory.go", model.PackageName)

// 基于模板创建文件（做好变量替换）
createFileFromStub(filePath, "factory", model)
}
```

## 2. 注册命令

app/cmd/make/make.go

```
.
.
.
func init() {
// 注册 make 的子命令
CmdMake.AddCommand(
.
.
.
CmdMakeFactory,
)
}
.
.
.
```

## 3. 模板

app/cmd/make/stubs/factory.stub

```
package factories

import (
"gohub/app/models/{{PackageName}}"
"gohub/pkg/helpers"

"github.com/bxcodec/faker/v3"
)

func Make{{StructNamePlural}}(count int) []{{PackageName}}.{{StructName}} {

var objs []{{PackageName}}.{{StructName}}

// 设置唯一性，如 {{StructName}} 模型的某个字段需要唯一，即可取消注释
// faker.SetGenerateUniqueValues(true)

for i := 0; i < count; i++ {
{{VariableName}}Model := {{PackageName}}.{{StructName}}{
FIXME()
}
objs = append(objs, {{VariableName}}Model)
}

return objs
}
```

## 4. 测试

执行命令：

```
$ go run main.go make factory project
[database/factories/project_factory.go] created.
```

请自行查看生成的文件。确认无误后请删除此文件：

```
$ rm -f database/factories/project_factory.go
```

## 代码版本

本节功能开发完毕。开始下一节之前，先来为代码做下版本标记：

```
$ git add .
$ git commit -m "make factory 命令"
```
