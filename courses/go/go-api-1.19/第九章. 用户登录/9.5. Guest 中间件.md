# 9.5. Guest 中间件

原文链接：https://learnku.com/courses/go-api/1.19/guest-middleware/13527

## 说明

这一节我们来创建 Guest 中间件。

Guest 中间件的用法与 Auth 中间件相反。用在一些游客身份才能操作的接口上，例如说用户注册、登录接口等。

使用方法同 Auth 中间件一样，在路由中做绑定，哪些路由只有游客才能访问，直接为其添加中间件即可。

## 创建中间件

app/http/middlewares/guest_jwt.go

```go
package middlewares

import (
	"gohub/pkg/jwt"
	"gohub/pkg/response"

	"github.com/gin-gonic/gin"
)

// GuestJWT 强制使用游客身份访问
func GuestJWT() gin.HandlerFunc {
	return func(c *gin.Context) {

		if len(c.GetHeader("Authorization")) > 0 {

			// 解析 token 成功，说明登录成功了
			_, err := jwt.NewJWT().ParserToken(c)
			if err == nil {
				response.Unauthorized(c, "请使用游客身份访问")
				c.Abort()
				return
			}
		}

		c.Next()
	}
}
```

## 测试一下

main.go 里：

main.go

```
.
.
.
bootstrap.SetupRoute(router)

router.GET("/test_guest", middlewares.GuestJWT(), func(c *gin.Context) {
c.String(http.StatusOK,  "Hello guest")
})

// 运行服务
.
.
.
```

创建一个测试的请求，尝试使用 Token 访问。应该会返回  401 未授权。

未使用 `Authorization:Bearer xxxxx` 访问，或者使用错误 Token ，就可以看到我们的 `Hello guest` 响应内容了。

## 删除测试代码

记得删除 main.go 里的这段测试代码：

```
router.GET("/test_guest", middlewares.GuestJWT(), func(c *gin.Context) {
c.String(http.StatusOK,  "Hello guest")
})
```

## 代码版本

本节功能开发完毕。开始下一节之前，先来为代码做下版本标记：

```bash
$ git add .
$ git commit -m "Guest 中间件"
```
