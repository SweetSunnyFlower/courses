# 17.4. 缓存友情链接列表

原文链接：https://learnku.com/courses/go-api/1.19/cache-links-list/13585

## 说明

本节课我们来缓存友情链接列表。

## 1. 添加辅助方法 link.AllCached

处理的缓存的逻辑不应该放到控制器里，一是保持控制器简洁，二是后续指不定其他地方也会用到 Link 的缓存数据。

app/models/link/link_util.go

```go
.
.
.
func AllCached() (links []Link) {
	// 设置缓存 key
	cacheKey := "links:all"
	// 设置过期时间
	expireTime := 120 * time.Minute
	// 取数据
	cache.GetObject(cacheKey, &links)

	// 如果数据为空
	if helpers.Empty(links) {
		// 查询数据库
		links = All()
		if helpers.Empty(links) {
			return links
		}
		// 设置缓存
		cache.Set(cacheKey, links, expireTime)
	}
	return
}
```

## 2. 调用缓存后的数据

修改 app/http/controllers/api/v1/links_controller.go 里的 Index 方法如下：

```go
func (ctrl *LinksController) Index(c *gin.Context) {
	response.Data(c, link.AllCached())
}
```

## 3. 测试

先修改 .env 文件里 LOG_LEVEL 项的值:

```
LOG_LEVEL=debug
```

这样我们能看到 SQL 请求，理论上应该是第一次接口请求会查询数据库，后面两个小时内的请求就会使用缓存里的。

Postman 里发送请求『友情链接列表』。

air 查看终端：

```
2022-01-06 17:48:13     DEBUG   link/link_util.go:25    Database Query  {"sql": "SELECT * FROM `links`", "time": "0.703ms", "rows": 5}
2022-01-06 17:48:13     DEBUG   middlewares/logger.go:65        HTTP Access Log {"status": 200, "request": "GET /v1/links", "query": "", "ip": "::1", "user-agent": "PostmanRuntime/7.28.4", "errors": "", "time": "1.700ms"}
```

再次请求，查看终端输出：

```
2022-01-06 17:48:40     DEBUG   middlewares/logger.go:65        HTTP Access Log {"status": 200, "request": "GET /v1/links", "query": "", "ip": "::1", "user-agent": "PostmanRuntime/7.28.4", "errors": "", "time": "0.512ms"}
```

第二次请求没有从数据库里拿数据。符合预期。

## 代码版本

本节功能开发完毕。开始下一节之前，先来为代码做下版本标记：

```bash
$ git add .
$ git commit -m "缓存友情链接列表"
```
