# 13.6. migrate reset 和 fresh 命令

原文链接：https://learnku.com/courses/go-api/1.19/migrate-reset-and-fresh-commands/13552

## 说明

这节课我们来创建两个命令

- migrate reset  —— 回溯所有迁移

- migrate fresh  —— reset 后重新之前 migrate up 命令

## 1. 添加 migrator 方法

pkg/migrate/migrator.go

```go
.
.
.
// Reset 回滚所有迁移
func (migrator *Migrator) Reset() {

	migrations := []Migration{}

	// 按照倒序读取所有迁移文件
	migrator.DB.Order("id DESC").Find(&migrations)

	// 回滚所有迁移
	if !migrator.rollbackMigrations(migrations) {
		console.Success("[migrations] table is empty, nothing to reset.")
	}
}

// Refresh 回滚所有迁移，并运行所有迁移
func (migrator *Migrator) Refresh() {

	// 回滚所有迁移
	migrator.Reset()

	// 再次执行所有迁移
	migrator.Up()
}
```

## 2. 添加命令

app/cmd/migrate.go

```go
.
.
.
var CmdMigrateReset = &cobra.Command{
	Use:   "reset",
	Short: "Rollback all database migrations",
	Run:   runReset,
}

func runReset(cmd *cobra.Command, args []string) {
	migrator().Reset()
}

var CmdMigrateRefresh = &cobra.Command{
	Use:   "refresh",
	Short: "Reset and re-run all migrations",
	Run:   runRefresh,
}

func runRefresh(cmd *cobra.Command, args []string) {
	migrator().Refresh()
}
```

## 3. 注册命令

app/cmd/migrate.go

```go
.
.
.
func init() {
    CmdMigrate.AddCommand(
        .
        .
        .
        CmdMigrateRefresh,
        CmdMigrateReset,
    )
}
.
.
.
```

## 测试

运行 migrate refresh 命令：

```bash
$ go run main.go migrate refresh
rollback 2022_01_05_200405_add_users_table
finsh 2022_01_05_200405_add_users_table
migrating 2022_01_05_200405_add_users_table
migrated 2022_01_05_200405_add_users_table
```

执行 migrate reset   命令：

```bash
$ go run main.go migrate reset
rollback 2022_01_05_200405_add_users_table
finsh 2022_01_05_200405_add_users_table
```

执行 migrate up  还原 users 表：

```bash
$ go run main.go migrate up
migrating 2022_01_05_200405_add_users_table
migrated 2022_01_05_200405_add_users_table
```

## 代码版本

本节功能开发完毕。开始下一节之前，先来为代码做下版本标记：

```bash
$ git add .
$ git commit -m "migrate reset 和 fresh 命令"
```
