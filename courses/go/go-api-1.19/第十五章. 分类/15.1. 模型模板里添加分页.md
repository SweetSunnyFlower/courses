# 15.1. 模型模板里添加分页

原文链接：https://learnku.com/courses/go-api/1.19/add-pagination-to-model-template/13566

## 说明

上一章我们为模型添加了 `Paginate()` 方法，这个功能其他模型可能会用到，我们将其添加到make model 的模板 model_util.stub 中。

## 添加到模板

app/cmd/make/stubs/model/model_util.stub

```go
package {{PackageName}}

import (
    "gohub/pkg/app"
    "gohub/pkg/database"
    "gohub/pkg/paginator"

    "github.com/gin-gonic/gin"
)
.
.
.
func Paginate(c *gin.Context, perPage int) ({{VariableNamePlural}} []{{StructName}}, paging paginator.Paging) {
    paging = paginator.Paginate(
        c,
        database.DB.Model({{StructName}}{}),
        &{{VariableNamePlural}},
        app.V1URL(database.TableName(&{{StructName}}{})),
        perPage,
    )
    return
}
```

注意顶部的 import 区块也要修改。

## 测试

执行命令：

```bash
$ go run main.go make model project
[app/models/project/project_model.go] created.
[app/models/project/project_util.go] created.
[app/models/project/project_hooks.go] created.
```

打开 app/models/project/project_util.go ，最后面：

```go
func Paginate(c *gin.Context, perPage int) (projects []Project, paging paginator.Paging) {
	paging = paginator.Paginate(
		c,
		database.DB.Model(Project{}),
		&projects,
		app.V1URL(database.TableName(&Project{})),
		perPage,
	)
	return
}
```

符合预期。

## 删除测试代码

```bash
$ rm -rf app/models/project
```

或者手动删除皆可。

## 代码版本

本节功能开发完毕。开始下一节之前，先来为代码做下版本标记：

```bash
$ git add .
$ git commit -m "模型模板里添加分页"
```
