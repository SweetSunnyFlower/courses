# 5.1. Larabbs 注册接口

原文链接：https://learnku.com/courses/laravel-weapp/1.7/larabbs-registration-interface/1569

本教程最新版为 [2.1](https://learnku.com/courses/laravel-weapp/2.1)，当前版本已放弃维护，请阅读最新版本！

## 注册

登录功能完成了，这一章我们来完成用户注册的功能，对于没有 Larabbs 账户的用户，可以使用手机注册。

## 增加 Larabbs 注册接口

Larabbs 中已经存在了用户手机注册的接口，但是并没有绑定微信小程序的功能，所以我们单独再写一个小程序手机注册接口。

## 增加路由

同小程序登录接口一样，我们使用 `weapp` 作为前缀，增加一个小程序注册接口。

routes/api.php

```
.
.
.
// 小程序登录
$api->post('weapp/authorizations', 'AuthorizationsController@weappStore')
->name('api.weapp.authorizations.store');
// 小程序注册
$api->post('weapp/users', 'UsersController@weappStore')
->name('api.weapp.users.store');
.
.
.
```

## 修改 Controller

app/Http/Controllers/Api/UsersController.php

```
.
.
.
public function weappStore(UserRequest $request)
{
// 缓存中是否存在对应的 key
$verifyData = \Cache::get($request->verification_key);

if (!$verifyData) {
return $this->response->error('验证码已失效', 422);
}

// 判断验证码是否相等，不相等反回 401 错误
if (!hash_equals((string)$verifyData['code'], $request->verification_code)) {
return $this->response->errorUnauthorized('验证码错误');
}

// 获取微信的 openid 和 session_key
$miniProgram = \EasyWeChat::miniProgram();
$data = $miniProgram->auth->session($request->code);

if (isset($data['errcode'])) {
return $this->response->errorUnauthorized('code 不正确');
}

// 如果 openid 对应的用户已存在，报错403
$user = User::where('weapp_openid', $data['openid'])->first();

if ($user) {
return $this->response->errorForbidden('微信已绑定其他用户，请直接登录');
}

// 创建用户
$user = User::create([
'name' => $request->name,
'phone' => $verifyData['phone'],
'password' => bcrypt($request->password),
'weapp_openid' => $data['openid'],
'weixin_session_key' => $data['session_key'],
]);

// 清除验证码缓存
\Cache::forget($request->verification_key);

// meta 中返回 Token 信息
return $this->response->item($user, new UserTransformer())
->setMeta([
'access_token' => \Auth::guard('api')->fromUser($user),
'token_type' => 'Bearer',
'expires_in' => \Auth::guard('api')->factory()->getTTL() * 60
])
->setStatusCode(201);
}
.
.
.
```

`UsersController` 中增加了 `weappStore` 方法， 逻辑同 `用户注册` 接口，但是需要提交微信的授权码 Code，通过 `$miniProgram->auth->session($request->code);` 获取微信的 `openId`，创建用户的同时绑定微信 `openId`，这样注册成功后下次用户即可直接登录，同时在 `meta` 中返回用户的 Token 信息。

## 代码版本控制

```
$ cd ~/Code/larabbs
$ git add -A
$ git commit -m 'weapp users store'
```
